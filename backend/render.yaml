services:
  - type: web
    name: plum-adjudicator-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: SUPABASE_URL
        value: https://dcrgkpvaxkmowxlyerhs.supabase.co
      - key: SUPABASE_KEY
        sync: false
